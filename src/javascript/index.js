'use strict';

//import instana from './instana_UI_3D/app';
//import setup from './instana_UI_3D/setup';
import resources from './ui_prototype/resources';
import data from './instana_data/dataListenerManager';

import instanaPrototype from './ui_prototype/app';

//first load all resources
//resources.load(function() { //on finished

  //start up the UI when all resources are loaded
  //var uiApplication = new instanaPrototype.App();// instana.Application();

  //test setup
  //setup(uiApplication); return;

  //var m = new data.DataListenerManager(uiApplication, 2000);
//});
