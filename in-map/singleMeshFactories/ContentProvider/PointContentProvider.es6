const VERTICES = [0, 0, 0];
const COLORS = [1, 1, 1];

const contentProvider = {
  getVertices: () => {
    return VERTICES;
  },

  getColors: () => {
    return COLORS;
  }
};

export default contentProvider;
