/*eslint-env mocha, node */
/*eslint-disable no-unused-expressions */


import {expect} from 'chai';


describe('3D map', () => {
  describe('application', () => {
    it('should init the application', () => {
      expect('doSomething').to.equal('doSomething');
    });
  });
});
