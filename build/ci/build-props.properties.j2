# This file is used to inject environment variables into the Jenkins
# build process. The file is translated via j2 and then loaded via a build
# step in the build job "build ui-client".
GIT_COMMIT={{GIT_COMMIT}}
INSTANA_UICLIENT_BRANCH={{INSTANA_UICLIENT_BRANCH}}
