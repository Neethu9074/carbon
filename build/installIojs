#!/bin/sh

set -eo pipefail

VERSION="iojs-v2.5.0"

# make sure to delete the old node modules first before changing the node
# version.
rm -rf node_modules/

nvm install "$VERSION"
nvm use "$VERSION"
nvm alias default $VERSION

npm install
