# CLI Tools

This package contains CLI tools that come with a slew of dependencies.
We extract these into this separate package so that we have fewer dependency
conflicts and upgrade issues with ui-client's main `package.json` (in the
root dir).
