export default class EventsGraphRenderer {

  constructor(buffer, height) {
    this.buffer = buffer;
    this.height = height;
  }

  setWidth(width) {
    this.width = width;
  }

  draw() {
    return;
  }
}
