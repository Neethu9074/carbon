import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action as storybookAction } from '@storybook/addon-actions';

import MetricSelectorOverlay from 'in-custom-dashboards/widgets/_shared/MetricConfigurator/sources/infrastructure/metrics/MetricSelectorOverlay';
import PermanentlyVisibleOverlay from '../util/PermanentlyVisibleOverlay';
import metricCatalogExampleJson from './metricCatalogExample';

<Meta title="Molecules|MetricSelectorOverlay" component={MetricSelectorOverlay} />

## Metric Selector

This overlay can be used within the query builder to allow selection of a metric by navigating through
the MetricTree.

<Preview>
  <Story name="default">
    <PermanentlyVisibleOverlay>
      <MetricSelectorOverlay
        onChange={storybookAction('onChange')}
        close={storybookAction('close')}
        metricCatalog={metricCatalogExampleJson}
      />
    </PermanentlyVisibleOverlay>
  </Story>
</Preview>

### Props

<Props of={MetricSelectorOverlay} />
