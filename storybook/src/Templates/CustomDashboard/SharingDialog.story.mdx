import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import SharingDialogPresenter from 'in-custom-dashboards/CustomDashboard/SharingDialog/SharingDialogPresenter';

<Meta title="Templates|CustomDashboard/SharingDialogPresenter" component={SharingDialogPresenter} />

<Preview>
  <Story name="default">
    <SharingDialogPresenter isPrivate setPrivate={action('setPrivate')} />
  </Story>
</Preview>

### Props
<Props of={SharingDialogPresenter} />
