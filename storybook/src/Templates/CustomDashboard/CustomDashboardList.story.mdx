import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';

import CustomDashboardListPresenter from 'in-custom-dashboards/CustomDashboardList/CustomDashboardListPresenter';
import { pendingResult, finishedProgress } from 'in-services/fixedObjects';

<Meta title="Templates|CustomDashboard/CustomDashboardList" component={CustomDashboardListPresenter} />

<Preview>
  <Story name="default">
    <CustomDashboardListPresenter customDashboards={{
      progress: finishedProgress,
      errors: [],
      data: [
        {
          id: '123',
          title: 'Data Ingestion'
        },
        {
          id: '456',
          title: 'Processing'
        },
        {
          id: '789',
          title: 'User-Interface Pipeline 🚙'
        }
      ]
    }} />
  </Story>
</Preview>

### Props
<Props of={CustomDashboardListPresenter} />

### Loading
<Preview>
  <Story name="Pending">
    <CustomDashboardListPresenter customDashboards={pendingResult} />
  </Story>
</Preview>

### Error
<Preview>
  <Story name="Error">
    <CustomDashboardListPresenter customDashboards={{
      progress: finishedProgress,
      errors: [{
        message: 'This error message must not be shown to users',
        code: 'SERVER'
      }]
    }} />
  </Story>
</Preview>

### No custom dashboards configured
<Preview>
  <Story name="No custom dashboards configured">
    <CustomDashboardListPresenter customDashboards={{
      progress: finishedProgress,
      errors: [],
      data: []
    }} />
  </Story>
</Preview>
