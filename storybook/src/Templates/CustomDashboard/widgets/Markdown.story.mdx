import { Meta, Story, Preview } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';

import {Widget, demo, Form, createForm} from 'in-custom-dashboards/widgets/Markdown';

import DemoDataStructure from './DemoDataStructure';

<Meta title="Templates|CustomDashboard/widgets/Markdown" component={Widget} />

# Introduction

The Markdown widget can be used to place arbitrary content onto a dashboard. This can be helpful to…

 - create a custom welcome message,
 - list common shell commands
 - and may other tings.

<Preview>
  <Story name="Widget with demo config">
    <Widget title="Hello World" config={demo}/>
  </Story>
</Preview>

# Form

The widget only requires a single `string` as its input which makes the form trivial.

<Preview>
  <Story name="Form based on demo config">
    <Form form={createForm(demo)} onChange={action('onChange')} />
  </Story>
</Preview>

# Configuration Data

The following is based on the `demo` data.

<DemoDataStructure config={demo} />
