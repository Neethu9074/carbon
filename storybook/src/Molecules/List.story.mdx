import { ColumnizedContent, Ul, Li, ListGroup } from 'in-new-components/lists/List';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import PermanentlyVisibleOverlay from '../util/PermanentlyVisibleOverlay';
import HealthDot from 'in-new-components/health/HealthDot/HealthDot';
import { action as storybookAction } from '@storybook/addon-actions';
import IconButton from 'in-new-components/IconButton/IconButton';
import CheckboxFancy from 'in-components/form/CheckboxFancy';
import { KeyValue } from '@instana/components';
import SparkChart from 'in-components/SparkChart';

export const columnDefinitions = [
  {
    width: '4rem',
    getContent() {
      return 'col1';
    }
  },
  {
    getContent({ label }) {
      return 'This is the first column which takes as much space as it is available but can ellipse' + ' ' + label;
    }
  },
  {
    width: '4rem',
    getContent() {
      return 'col2';
    }
  },
  {
    width: '4rem',
    getContent() {
      return 'col3';
    }
  }
];

<Meta title="Molecules|List" />

## Simple List

<Preview>
  <Story name="default">
    <Ul>
      <Li>Item 1</Li>
      <Li>Item 2</Li>
      <Li>Item 3</Li>
      <Li>Item 4</Li>
      <Li>Item 5</Li>
    </Ul>
  </Story>
</Preview>

## Import

```js
import { ColumnizedContent, Ul, Li } from 'in-new-components/lists/List';
```

## Clickable List

You can add an `onClick` prop to each `Li` to make it clickable. Please consider using the `href` and `href$`
alternatives for improved usability, e.g. *open in new tab* support. Use `onDefaultHrefInteractionSideEffect` for
any kind of non-navigational side-effects.

<Preview>
  <Story name="Clickable List">
    <Ul>
      <Li onClick={storybookAction('click')}>Item with onClick prop</Li>
      <Li href="javascript:alert('Hello from href prop')">Item with href prop</Li>
      <Li href="javascript:alert('Hello from href prop 2!')"
          onDefaultHrefInteractionSideEffect={() => alert('Side Effect!')}>
        Item href prop and a side-effect before the link is followed. Use this to close overlays, report tracking
        information to Mixpanel etc.
      </Li>
    </Ul>
  </Story>
</Preview>


## Columized List

<Preview>
  <Story name="Columized List">
    <Ul>
      <Li>
        <ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" />
      </Li>
      <Li>
        <ColumnizedContent columnDefinitions={columnDefinitions} label="label 2" />
      </Li>
      <Li>
        <ColumnizedContent columnDefinitions={columnDefinitions} label="label 3" />
      </Li>
    </Ul>
  </Story>
</Preview>


## Expandable Content

It is possible to add expandable content to lists. We currently support two kinds of expandable (sub-) content:

 1. Sub-lists with increasing levels in indentation.
 2. Free-form sub-content.

By default expandable content is only shown when the small icon is clicked. You may however change this default
behavior to *expand on row click* using the `toggleContentOnRowClick` prop.

Use the `initiallyOpen` prop to expand content by default.

<Preview>
  <Story name="Expandable Content">
    <Ul>
      <Li initiallyOpen subList={
        <Ul>
          <Li subList={
            <Ul>
              <Li>Sub Sub Item 1</Li>
              <Li>Sub Sub Item 2</Li>
              <Li>Sub Sub Item 3</Li>
            </Ul>
          }>
            Sub Item 1
          </Li>
          <Li subList={
            <Ul>
              <Li>Sub Sub Item 1</Li>
              <Li>Sub Sub Item 2</Li>
              <Li>Sub Sub Item 3</Li>
            </Ul>
          }>
            Sub Item 2
          </Li>
          <Li subList={
            <Ul>
              <Li>Sub Sub Item 1</Li>
              <Li>Sub Sub Item 2</Li>
              <Li>Sub Sub Item 3</Li>
            </Ul>
          }>
            Sub Item 3
          </Li>
        </Ul>
      }>
        Sub-lists with increasing levels of indentation
      </Li>
      <Li renderNestedContent={() => (<span>Nested Content!</span>)}>
        Free-form exandable content
      </Li>
      <Li toggleContentOnRowClick
          renderNestedContent={() => (<span>Nested Content!</span>)}>
        Exandable content toggled on row click
      </Li>
    </Ul>
  </Story>
</Preview>

## List Groups

<Preview>
  <Story name="List Groups">
    <Ul>
      <ListGroup label="Group 1">
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
      </ListGroup>
      <ListGroup label="Group 2">
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
      </ListGroup>
    </Ul>
  </Story>
</Preview>

## Top Bottom List Groups

<Preview>
  <Story name="Top Bottom List Groups">
    <Ul framed="topBottom">
      <ListGroup label="Group 1">
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
      </ListGroup>
      <ListGroup label="Group 2">
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
        <Li><ColumnizedContent columnDefinitions={columnDefinitions} label="label 1" /></Li>
      </ListGroup>
    </Ul>
  </Story>
</Preview>

## ItemList

export const columns = [
  {
    width: '2rem',
    getContent() {
      return (<CheckboxFancy checked={true} size="large"/>);
    }
  },
  {
    width: '3rem',
    getContent() {
      return (<IconButton key="someKey1" type="lib_views_tag"></IconButton>);
    }
  },
  {
    width: '2rem',
    getContent() {
      return (<HealthDot severity={0} iconSize={10} />);
    }
  },
  {
    getContent() {
      return (<KeyValue label="host.tag.fleet" value="AXT-12" accentuated />);
    }
  },
  {
    width: '6rem',
    getContent() {
      return (<KeyValue label="Hosts" value="3" theme="blue" accentuated />);
    }
  },
  {
    width: '12rem',
    getContent() {
      return (
        <SparkChart
          rollup={10000}
          timeConfig={{ windowSize: 60000, to: 60000 }}
          metrics={[[0, 200], [10000, 192], [20000, 197], [30000, 220], [40000, 197], [50000, 800], [60000, 198]]}
          horizontalMetricValue="8.62%"
          label="Memory.usage"
        />
      );
    }
  },
  {
    width: '12rem',
    getContent() {
      return (
        <SparkChart
          rollup={10000}
          timeConfig={{ windowSize: 60000, to: 60000 }}
          metrics={[[0, 200], [10000, 192], [20000, 197], [30000, 220], [40000, 197], [50000, 800], [60000, 198]]}
          horizontalMetricValue="8.62%"
          label="Memory.usage"
          theme="light"
        />
      );
    }
  },
  {
    width: '3rem',
    getContent() {
      return (<IconButton key="someKey" type="lib_menu_more_horizontal"></IconButton>);
    }
  }
];

<Preview>
  <Story name="ItemList">
    <Ul space="xsmall">
      <Li noAlternatingBg
          borderRadius="medium"
          toggleContentOnRowClick
          highlightOpenState={false}
          renderNestedContent={() => (<span>Content</span>)}>
        <ColumnizedContent columnDefinitions={columns} label="AXT-12" />
      </Li>
      <Li noAlternatingBg
          borderRadius="medium"
          toggleContentOnRowClick
          highlightOpenState={false}
          renderNestedContent={() => (<span>Content</span>)}>
        <ColumnizedContent columnDefinitions={columns} label="AXT-12" />
      </Li>
      <Li noAlternatingBg
          borderRadius="medium"
          toggleContentOnRowClick
          highlightOpenState={false}
          renderNestedContent={() => (<span>Content</span>)}>
        <ColumnizedContent columnDefinitions={columns} label="AXT-12" />
      </Li>
      <Li noAlternatingBg
          borderRadius="medium"
          toggleContentOnRowClick
          highlightOpenState={false}
          renderNestedContent={() => (<span>Content</span>)}>
        <ColumnizedContent columnDefinitions={columns} label="AXT-12" />
      </Li>
    </Ul>
  </Story>
</Preview>

## Stylistic Variants

### Rounded Corners

You can ask the `List` to render itself with rounded corners. This is helpful when the `List` is supposed to be
rendered inside `Overlay`s.

Please note that the `List` only supports `medium` as a valid `borderRadius`. The `borderRadius` mode is also
currently not fully supported when using sub-lists or nested content.

<Preview>
  <Story name="Rounded Corners (sub lists)">
    <PermanentlyVisibleOverlay>
      <Ul borderRadius="medium" style={{maxWidth: '10rem'}}>
        <Li>A</Li>
        <Li>B</Li>
        <Li>C</Li>
      </Ul>
    </PermanentlyVisibleOverlay>
  </Story>
</Preview>
