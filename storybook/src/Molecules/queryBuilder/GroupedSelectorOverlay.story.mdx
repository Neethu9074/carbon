import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action as storybookAction } from '@storybook/addon-actions';

import GroupedSelectorOverlay from 'in-new-components/QueryBuilder/GroupedSelectorOverlay/GroupedSelectorOverlay';
import PermanentlyVisibleOverlay from '../../util/PermanentlyVisibleOverlay';

<Meta title="Molecules|queryBuilder/GroupedSelectorOverlay" component={GroupedSelectorOverlay} />

## Conjunction Selector

This overlay can be used within the query builder to allow selection of a
conjunction, braces or to even clear the selection.

<Preview>
  <Story name="default">
    <PermanentlyVisibleOverlay>
      <GroupedSelectorOverlay value="2" onChange={storybookAction('onChange')} close={storybookAction('close')} items={[
        {
          value: "1",
          label: "catalogue/*",
          items: [
            {
              value: "1.1",
              label: "catalogue/GET/product/{sku}",
              items: [
                {
                  value: "1.1.1",
                  label: "catalogue/GET/products",
                }
              ]
            }
          ]
        },
        {
          value: "2",
          label: "users/*",
          items:[
            {
              value: "2.1",
              label: "users/2fa"
            },
            {
              value: "2.2",
              label: "users/3fa"
            },
            {
              value: "2.3",
              label: "users/4fa"
            }
          ]
        },
        {
          value: "3",
          label: "settings/*",
        }
      ]}/>
    </PermanentlyVisibleOverlay>
  </Story>
</Preview>

### Props
<Props of={GroupedSelectorOverlay} />

