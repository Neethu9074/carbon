import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action as storybookAction } from '@storybook/addon-actions';

import OperatorSelectorOverlay from 'in-components/QueryBuilder/OperatorSelectorOverlay/OperatorSelectorOverlay';
import { STRING as allowedOperators } from 'in-components/QueryBuilder/tagFilter/typeToOperatorsMapping';
import { CONTAINS } from 'in-components/QueryBuilder/tagFilter/operators';
import PermanentlyVisibleOverlay from '../../util/PermanentlyVisibleOverlay';
import { STRING } from 'in-components/QueryBuilder/tagFilter/types';

<Meta title="Molecules|queryBuilder/OperatorSelectorOverlay" component={OperatorSelectorOverlay} />

## Conjunction Selector

This overlay can be used within the query builder to allow selection of a
conjunction, braces or to even clear the selection.

<Preview>
  <Story name="default">
    <PermanentlyVisibleOverlay>
      <OperatorSelectorOverlay value={CONTAINS} allowedOperators={allowedOperators} tagType={STRING} onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Preview>

### Props
<Props of={OperatorSelectorOverlay} />

