import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action as storybookAction } from '@storybook/addon-actions';

import TagSelectorOverlay from 'in-new-components/QueryBuilder/TagSelectorOverlay/TagSelectorOverlay';
import PermanentlyVisibleOverlay from '../../util/PermanentlyVisibleOverlay';

<Meta title="Molecules|queryBuilder/TagSelectorOverlay" component={TagSelectorOverlay} />

## Conjunction Selector

This overlay can be used within the query builder to allow selection of a
conjunction, braces or to even clear the selection.

<Preview>
  <Story name="default">
    <PermanentlyVisibleOverlay>
      <TagSelectorOverlay onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Preview>

### Props
<Props of={TagSelectorOverlay} />

