import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { action as storybookAction } from '@storybook/addon-actions';

import MetricConfigurator from 'in-new-components/MetricConfigurator/MetricConfigurator';

## MetricConfigurator

<Meta title="Molecules|MetricConfigurator" component={MetricConfigurator} />

export const options = [
  {
    metric: 'beaconCount',
    label: 'This is intentionally a very very long name',
    description: 'How many beacons matching a filter were recorded.',
    aggregations: ['SUM']
  },
  {
    metric: 'beaconDuration',
    label: 'Beacon duration',
    description: 'The recorded duration for a beacon.',
    aggregations: ['P99', 'MEAN', 'P25']
  },
  {
    metric: 'pageLoads',
    label: 'Page loads',
    description:
      'How often a full page load occurred. This typically requires retrieval of an HTML document from a server (unless cached) and then the full onLoad cycle.',
    aggregations: ['SUM']
  }
];

export function generateOptions() {
  let result = [];
  for (let index = 0; index < 15000; index++) {
    result.push({
      metric: 'beaconDuration' + index,
      label: 'Beacon duration ' + index,
      description: 'The recorded duration for a beacon.',
      aggregations: ['P99', 'MEAN', 'P25', 'MIN', 'MAX', 'P90', 'SUM', 'P50', 'P95', 'P98', 'P75']
    });
  }
  return result;
}

<Preview>
  <Story name="default">
    <MetricConfigurator
      options={options}
      values={[]}
      onChange={storybookAction('onChange')}
    />
  </Story>
</Preview>

export const aLotOfOptions = generateOptions();

<Preview>
  <Story name="Example with 15k metrics">
    <MetricConfigurator
      options={aLotOfOptions}
      values={[]}
      onChange={storybookAction('onChange')}
    />
  </Story>
</Preview>

### Props

<Props of={MetricConfigurator} />
