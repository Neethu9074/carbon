import { storiesOf } from '@storybook/react';
import React from 'react';

import HttpFilterSelect from 'in-components/HttpFilterSelect';

storiesOf('old_components/MultiSelect', module).add('Http Filters', () => <HttpFilterSelect />);
