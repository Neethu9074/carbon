@import '~in-themes/active.less';

:local {
  .wrapper,
  .chartWrapper {
    border-top: 1px solid @lib__colors__N400;
  }

  .chartWrapper {
    padding: 1rem;

    background: @lib__colors__white;
  }
}
