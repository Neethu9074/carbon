@import '~in-themes/mixins.less';

:local {
  .link {
    .ellipsis();
  }

  .metricLabel {
    display: inline-block;
    width: 100%;
    padding-right: 0.5rem;
    vertical-align: bottom;
    .ellipsisLeft();
  }
}
