@import '~in-themes/mixins.less';

:local {
  .metricLabel {
    display: inline-block;
    padding-right: 0.5rem;
    vertical-align: bottom;
    .ellipsisLeft();
  }

  .entityLink {
    display: flex;
    flex-direction: row;
    align-items: center;
  }

  .pill {
    margin-left: 1rem;
  }
}
