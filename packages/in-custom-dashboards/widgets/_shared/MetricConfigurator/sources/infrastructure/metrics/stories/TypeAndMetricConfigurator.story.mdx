import { Meta, Story, Preview, Props } from '@storybook/addon-docs';

import TypeAndMetricConfigurator from 'in-custom-dashboards/widgets/_shared/MetricConfigurator/sources/infrastructure/metrics/TypeAndMetricConfigurator'

<Meta component={TypeAndMetricConfigurator} />

export function generateError() {
  return {errors: [{message: "we have an error", code: "SERVER"}]};
}

export const errorResponse = generateError();

<Preview>
  <Story name="ResultWithError">
    <TypeAndMetricConfigurator
      metricCatalog={errorResponse}
    />
  </Story>
</Preview>
