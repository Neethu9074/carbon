import React from 'react';

import Info from '../Info';

export default function AzureSqlDatabaseSidebarDetails({ snapshot }) {
  return (
    <div>
      <Info snapshot={snapshot} />
    </div>
  );
}
