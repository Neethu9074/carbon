// This module existts for better code splitting support. Without this
// the whole trace view would land in the entry bundle (via the shortcut init handler)
export const leftArrowId = 'navigator-previous-item';
export const rightArrowId = 'navigator-next-item';
