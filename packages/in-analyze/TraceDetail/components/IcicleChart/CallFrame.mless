/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */
@import '~in-themes/mixins.less';

:local {
  .frame {
    position: absolute;

    display: flex;
    align-items: center;

    min-width: 5px;
    cursor: pointer;
    border: 1px solid #fff;

    &:hover {
      opacity: 0.75;
    }
  }

  .isOpened {
    font-weight: bold;
    opacity: 0.8;
  }

  .unhighlightedFrame {
    opacity: 0.2;
  }

  .fakeRoot {
    cursor: default;
  }

  .label {
    display: inline-block;
    color: white;
    margin-left: 0.4rem;
    .ellipsis();
  }

  .errorIndicator {
    margin-top: -24px;
    margin-left: -0.5rem;
  }

  .unhighlightedLogIndicator {
    opacity: 0.2;
  }
}
