:local {
  .frame {
    position: absolute;

    display: flex;
    align-items: center;

    min-width: 5px;
    cursor: pointer;
    border: 1px solid #fff;

    &:hover {
      opacity: 0.75;
    }
  }

  .unhighlightedFrame {
    opacity: 0.2;
  }

  .fakeRoot {
    cursor: default;
  }

  .label {
    display: inline-block;
    color: white;
    margin-left: 0.4rem;
    overflow: hidden;
    white-space: nowrap;
  }

  .errorIndicator {
    margin-top: -24px;
    margin-left: -0.5rem;
  }
}
