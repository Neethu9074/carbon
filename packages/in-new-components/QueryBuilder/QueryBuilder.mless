:local {
  .queryBuilder {
    display: flex;
    flex-wrap: wrap;
    align-items: center;

    // TODO this must be supported on input elements. Reconsider this approach?
    user-select: none;
  }
}
