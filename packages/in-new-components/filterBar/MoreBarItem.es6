import React from 'react';

import BarItem from 'in-new-components/filterBar/BarItem/BarItem';

export default function MoreBarItem({ onClick }) {
  return <BarItem onClick={onClick}>More</BarItem>;
}
