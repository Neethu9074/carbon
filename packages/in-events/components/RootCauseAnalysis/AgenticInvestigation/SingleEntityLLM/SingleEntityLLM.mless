:local {
  .llmContainer {
    padding-top: var(--cds-spacing-05);
  }

  .aiLabel {
    // &:global .cds--popover-c
    &:global(.cds--ai-label:has(> .cds--popover--open)) {
      z-index: 3;
    }
  }
}
