@import '~in-themes/active.less';

:local {
  .container {
    display: inline-flex;
    margin: 0.25rem 0;
  }

  .label {
    font-weight: bold;
    font-size: 0.75rem;
    margin-right: 0.25rem;
    text-transform: capitalize;
  }

  .subEntity {
    display: inline-flex;
    align-items: center;
  }
}
