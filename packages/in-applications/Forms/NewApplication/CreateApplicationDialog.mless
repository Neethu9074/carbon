@import '~in-themes/active.less';

:local {
  .applicationNameText {
    margin-top: 1rem;
  }

  .filter {
    margin: 0.25rem;
  }

  .matchSspecifications {
    display: flex;
    align-items: center;
    flex-direction: column;
  }
}
