@import '~in-themes/active.less';

:local {
  .filter {
    margin: 0 0.5rem 0 0;
    z-index: 4;
  }

  .flexWrapper {
    display: flex;
    align-items: center;
  }

  .linkEntityIcon {
    margin-right: 0.25rem;
    fill: @lib__colors__blue800;
  }
}
