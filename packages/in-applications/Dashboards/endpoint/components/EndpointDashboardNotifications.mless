@import '~in-themes/active.less';

:local {
  .wrapper {
    margin-bottom: 1rem;
  }

  .messageWrapper {
    line-height: 1.1rem;
    margin-bottom: 0.5rem;
  }

  .link {
    float: right;
    color: @lib__colors__teal800;

    &:hover {
      text-decoration: none;
    }
  }
}
