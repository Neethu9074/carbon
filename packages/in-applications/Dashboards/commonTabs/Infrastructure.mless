@import '~in-themes/active.less';

:local {
  .cell {
    display: inline-flex;
    align-items: center;
    cursor: not-allowed;
    color: @lib__colors__N500;
  }

  .simplePluginIcon {
    margin-right: 0.5rem;
    fill: @lib__colors__N500;
  }
}
