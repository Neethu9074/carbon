@import '~in-themes/active.less';

:local {
  .filterWrapper {
    grid-column: 1 / span 2;
    padding: 0 .5rem;
    margin-top: .5rem 0 1rem;
    overflow-y: scroll;
  }

  .descriptionText {
    font-size: .8rem;
    line-height: 1.5;
  }
}
