@import '~in-themes/active.less';

:local {
  .listViewHeader {
    display: flex;
    align-items: flex-end;

    height: 4.5rem;
    padding-bottom: 8px;

    background: @lib__colors__navy800;
    color: white;
    font-size: 22px;
    font-weight: lighter;
    overflow: hidden;
  }
}
