@import '~in-themes/active.less';

:local {
  .matchSpecification {
    display: flex;
    align-items: flex-start;

    & + .matchSpecification {
      margin-top: 1rem;
    }
  }

  .matchHelp {
    margin: 0 0 1rem;
  }

  .formGroup {
    margin: 0;
    width: 100%;

    & + .formGroup {
      margin-left: 1rem;
    }
  }

  .removeMatchRule {
    margin: 1.4rem 0 0 1rem;
  }

  .matchingCard {
    margin: @grid__gutter * 1px 0 0;
  }

  .actions {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    margin: @grid__gutter * 1px 0 0;
  }

  .saveErrors {
    margin: 0 1rem 0 0;
  }
}
