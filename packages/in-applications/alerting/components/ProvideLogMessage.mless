@import '~in-themes/active.less';

:local {
  .container {
    margin: 0.5rem;
  }

  .logMessageSelectWrapper {
    > p {
      margin-bottom: 0.5rem;
    }
  }

  .logMessageSelectAdvanceMode {
    align-self: flex-end;

    > button {
      float: right;
    }
  }
}
