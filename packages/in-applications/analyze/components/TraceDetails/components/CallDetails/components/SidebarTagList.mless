/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

@import '~in-themes/mixins.less';

:local {
  .key,
  .rightAlignedValue,
  .leftAlignedValue {
    width: 50%;
    .ellipsis();
  }

  .key {
    text-align: left;
  }

  .rightAlignedValue {
    text-align: right;
  }

  .leftAlignedValue {
    text-align: left;
  }

  .root {
    position: relative;
  }

  .root:hover .clipboard {
    display: block;
  }

  .clipboard {
    display: none;
    position: absolute;
    right: 0.5rem;
  }

  .tagsCard {
    margin-top: 1.5rem;
  }
}
