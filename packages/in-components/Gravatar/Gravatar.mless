/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

:local {
  .regular,
  .l {
    border-radius: 50%;
    background: var(--ids-color-option-neutral-200);
  }

  .regular {
    .size(2rem);
  }

  .l {
    .size(3rem);
  }

  .size(@size) {
    & {
      min-width: @size;
      min-height: @size;
      max-width: @size;
      max-height: @size;
    }
  }
}
