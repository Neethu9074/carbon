import { Meta, Story, Canvas, Props } from '@storybook/addon-docs';
import { action as storybookAction } from '@storybook/addon-actions';

import SourceDestinationSelectorOverlay from 'in-components/QueryBuilder/SourceDestinationSelectorOverlay/SourceDestinationSelectorOverlay';
import {destination} from 'in-components/QueryBuilder/SourceDestinationSelectorOverlay/supportedSelections';
import PermanentlyVisibleOverlay from 'in-components/overlays/OverlayPresenter/stories/PermanentlyVisibleOverlay';

<Meta component={SourceDestinationSelectorOverlay} />

## Source/Destination Selector

This overlay can be used within the query builder to allow selection of a
source/destination for the application product area.

<Canvas>
  <Story name="default">
    <PermanentlyVisibleOverlay>
      <SourceDestinationSelectorOverlay value={destination} onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Canvas>

<Canvas>
  <Story name="sourceEnabled={true}">
    <PermanentlyVisibleOverlay>
      <SourceDestinationSelectorOverlay sourceEnabled={true} value={destination} onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Canvas>

<Canvas>
  <Story name="destinationEnabled={true}">
    <PermanentlyVisibleOverlay>
      <SourceDestinationSelectorOverlay destinationEnabled={true} value={destination} onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Canvas>

<Canvas>
  <Story name="sourceEnabled={true} destinationEnabled={true}">
    <PermanentlyVisibleOverlay>
      <SourceDestinationSelectorOverlay sourceEnabled={true} destinationEnabled={true} value={destination} onChange={storybookAction('onChange')} close={storybookAction('close')} />
    </PermanentlyVisibleOverlay>
  </Story>
</Canvas>
