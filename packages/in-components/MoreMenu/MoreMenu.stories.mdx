import { Meta, Story, Canvas, Props } from '@storybook/addon-docs';
import { action as storybookAction } from '@storybook/addon-actions';
import { MoreMenu, MoreMenuButton } from 'in-components/MoreMenu';
import { stopPropagation } from 'in-services/util/function';
import { Button } from '@instana/components';

<Meta component={MoreMenu} />

## MoreMenu

Use `MoreMenu` to add an expandable section that reveals several actions/navigation paths that users can take.

**Do not use `MoreMenu` to implement comboboxes!** See `Combobox` instead which allows custom combobox-like implementations with good default UI/UX.

<Canvas>
  <Story name="default">
    <MoreMenu>
      <MoreMenuButton icon="lib_actions_maximize">TV Mode</MoreMenuButton>
      <MoreMenuButton icon="lib_views_grid">Make Homepage</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_edit">Edit Name</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_copy">Duplicate</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_delete">Delete</MoreMenuButton>
    </MoreMenu>
  </Story>
</Canvas>

## MoreMenu

Use `MoreMenu` to add an expandable section that reveals several actions/navigation paths that users can take.

<Canvas>
  <Story name="with custom interactive element to open the menu">
    <MoreMenu
        renderInteractiveElement={({ ref, toggle }) => (
          <Button
            kind="action"
            onClick={e => {
              stopPropagation(e);
              toggle();
            }}
            ref={ref}
          >Trigger action</Button>
        )}
    >
      <MoreMenuButton icon="lib_actions_maximize">TV Mode</MoreMenuButton>
      <MoreMenuButton icon="lib_views_grid">Make Homepage</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_edit">Edit Name</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_copy">Duplicate</MoreMenuButton>
      <MoreMenuButton icon="lib_actions_delete">Delete</MoreMenuButton>
    </MoreMenu>
  </Story>
</Canvas>
