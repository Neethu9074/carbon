:local {
  .signOutButton {
    height: 3.5rem;
    cursor: pointer;
    .emailAddress {
      color: var(--cds-text-secondary);
      font-size: var(--ids-font-size-option-body-small);
    }
    :global(.cds--side-nav__item) {
      height: 100%;
    }
    :global(.cds--side-nav__item a.cds--side-nav__link) {
      height: 100%;
    }
  }

  :global(.shell-wrapper .cds--side-nav__navigation .cds--tag--blue) {
    --cds-tag-background-blue: #0043ce;
    --cds-tag-color-blue: #d0e2ff;
  }
  :global(#profileMenu-switcher) {
    button {
      padding: 0;
      svg {
        top: 0;
        bottom: 0;
      }
    }
  }
}
