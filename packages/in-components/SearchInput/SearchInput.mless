/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

@import '~in-themes/mixins.less';

:local {
  .wrapper {
    display: flex;
    align-items: center;
    position: relative;

    background: var(--ids-color-option-neutral-50);
  }

  .wrapperDisabled {
    cursor: not-allowed;
  }

  .icon {
    fill: var(--ids-color-option-neutral-500);
    position: absolute;
    right: 0.5rem;
  }

  .iconDisabled {
    fill: var(--ids-color-option-neutral-400);
  }

  .iconHidden {
    display: none;
  }

  .searchInput {
    width: 100%;
    min-width: 0; // allow flex-item to shrink below it's "default" width

    &:focus {
      outline: none;
    }
  }

  .hasError {
    background: #ff40401f;
  }

  .useTransparency {
    background: transparent;
  }

  .hasFocus {
    .browserDefaultFocusStyles();
  }
}
