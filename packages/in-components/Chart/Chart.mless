:local {
  .wrapper {
    width: 100%;
  }

  .chart {
    position: relative;
    width: fit-content;
  }

  .canvas {
    display: block;
  }

  .chartAxisWrapper {
    display: flex;
    align-items: flex-start;
  }
}
