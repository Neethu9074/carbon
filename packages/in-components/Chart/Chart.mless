:local {
  .wrapper {
    width: 100%;
    margin: 0 0 1rem 0;
  }

  .chart {
    position: relative;
    width: fit-content;
  }

  .canvas {
    width: 100%;
  }

  .canvas,
  .timeAxisCanvas {
    display: block;
  }

  .timeAxisCanvas {
    height: 3.125rem;
  }

  .chartAxisWrapper {
    position: relative;
    display: flex;
    align-items: flex-start;
  }
}
