/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

@import '~in-themes/mixins.less';

:local {
  .wrapper {
    display: flex;
    align-items: center;
    justify-content: space-between;
    color: var(--ids-color-option-neutral-500);
    font-family: var(--ids-font-family-option-sans-serif);

    border: 1px solid var(--ids-color-option-neutral-500);
    border-top: 0;
    background: #fff;

    &:first-child {
      border-top: 1px solid var(--ids-color-option-neutral-500);
      border-top-left-radius: var(--ids-border-radius-option-small);
      border-top-right-radius: var(--ids-border-radius-option-small);
    }

    &:last-child {
      border-bottom-left-radius: var(--ids-border-radius-option-small);
      border-bottom-right-radius: var(--ids-border-radius-option-small);
    }
  }

  .line {
    display: flex;
    align-items: center;
    overflow: hidden;
    padding: 0.45rem 0.625rem;
  }

  .filler {
    flex-shrink: 0;
    margin: 0 0.4rem;
  }

  .withoutExtraWhitespace {
    margin: 0;
  }

  .value {
    color: var(--ids-color-option-neutral-900);
    line-height: 1.2;
    .ellipsis();
  }

  .noShrinking {
    flex-shrink: 0;
  }

  .file {
    flex-shrink: 2;
  }

  .name {
    flex-shrink: 0.5;
    font-weight: 600;
  }
}
