/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

:local {
  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0.5rem 0;
  }

  .carbonHeader {
    display: flex;
    align-items: center;
    justify-content: space-between;
    position: relative;
    z-index: 2;
    :global(.card-header) {
      padding-bottom: 0;
    }
  }

  .rightHeader {
    display: flex;
    align-items: center;
    justify-content: flex-end;
  }

  .carbonRightHeader {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    z-index: 2;
  }

  .paginationWrapper {
    display: flex;
    justify-content: center;

    width: 100%;

    margin: 1rem 0;
  }

  .scrollableTable {
    overflow-x: auto;
  }

  .title {
    display: flex;
    font-size: 1.375rem;
    margin-top: 1.5rem;
    font-weight: 500;
  }

  .carbonTitle {
    :global(.card-header) {
      padding-bottom: 0.75rem;
    }
    :global(.title) {
      margin-top: 0;
    }
  }

  .carbonLeftHeader {
    > h1 {
      margin-top: 0;
    }
  }

  .shadowless {
    box-shadow: none;
  }

  .tableContainer {
    :global(.cds--data-table-container) {
      padding: 0;
    }
  }

  .emptyTable {
    :global(.table-row) {
      display: grid;
    }
  }

  .tableMinimumHorizontalSpace {
    white-space: nowrap;
  }

  .tableTdNoWrap {
    white-space: nowrap;
  }

  .tableEllipsis {
    white-space: nowrap;
    text-overflow: ellipsis;
    overflow: hidden;
  }

  .noPadding {
    padding: var(--cds-layout-density-padding-inline-local) 0;
  }

  .carbonNoTitle {
    :global(.card-header) {
      padding-bottom: 0;
    }
    padding-top: 0;
  }
}
