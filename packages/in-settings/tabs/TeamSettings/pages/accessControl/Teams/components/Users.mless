@import '~in-themes/mixins.less';

:local {
  .allProperties {
    display: flex;
    flex-direction: row;
    overflow-x: hidden;
  }

  .narrowColumn {
    width: 4rem;
  }

  .ellipsis {
    .ellipsis();
  }
}
