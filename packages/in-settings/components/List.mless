/*
 * (c) Copyright IBM Corp. 2021
 * (c) Copyright Instana Inc.
 */

:local {
  .createNewButton {
    margin-right: 1.5rem;
  }

  a:not([href]):not([tabindex]).createNewButton {
    color: var(--ids-color-option-teal-500);
  }

  :global(.g10) a:not([href]):not([tabindex]).createNewButton {
    color: var(--cds-link-primary);
  }

  .selectCheckbox {
    padding-top: 0.95rem !important;
    flex-direction: row;
    justify-content: center;
    align-items: center;
  }

  .selectCheckboxHead {
    width: 2rem;
  }

  .headerWithSelectAllButton {
    margin-right: 0.5rem;
  }

  .deleteWrapper {
    text-align: right;
  }
  .withBottomPadding {
    padding-bottom: 5rem;
  }
}
