@import '~in-themes/active.less';

:local {
  .tooltip {
    min-width: 18rem;
  }

  .headlingFlexWrapper,
  .flexWrapper {
    display: flex;
    justify-content: space-between;
  }

  .headingFlexWrapper {
    color: @lib__colors__N500;
  }
}
