@import '~in-themes/active.less';

:local {
  .tooltip {
    min-width: 18rem;
  }

  .podPhaseLabel {
    margin-bottom: 1rem;
  }

  .headlingFlexWrapper,
  .flexWrapper {
    display: flex;
    justify-content: space-between;
  }

  .headlingFlexWrapper {
    color: @lib__colors__N500;
  }
}
