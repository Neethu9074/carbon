/*
 * IBM Confidential
 * PID 5737-N85, 5900-AG5
 * Copyright IBM Corp. 2025
 */

:local {
  .bannerSection {
    margin-left: -1.5rem;
    margin-right: -1.5rem;
    margin-top: -1.5rem;
    background: var(--ids-color-option-neutral-100);
  }

  .banner {
    display: flex;
    width: 100%;
    position: relative;

    > div {
      flex: 0 0 35%;
    }

    :nth-child(2) {
      flex: 1;
    }

    &:before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      height: 100%;
      width: 100%;
      background: linear-gradient(
        180deg,
        rgba(138, 63, 252, 0.1) 0%,
        rgba(69, 137, 255, 0.03) 51.21%,
        rgba(255, 255, 255, 0) 128.32%
      );
      z-index: 0;
      pointer-events: none;
    }
    :global .cds--tag {
      margin-left: 0.5rem;
      vertical-align: top;
    }
  }

  .leftContainer {
    margin-top: 3rem;
    margin-left: 3rem;
  }

  .product {
    > svg {
      margin-right: 0.5rem;
      vertical-align: text-bottom;
    }
  }

  .illustration {
    display: flex;
    justify-content: flex-end;
    margin-right: 3rem;
  }

  .buttonSection {
    display: flex;
    justify-content: right;
    background: var(--ids-color-option-neutral-100);
  }
  .cutOffText {
    hyphens: none;
  }

  .collapsibleContainer {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .leftCollapsibleTag {
    margin-top: 1rem;
    margin-left: 2rem;
    padding-bottom: 0.5rem;

    :global .cds--tag {
      margin-left: 0.5rem;
      vertical-align: top;
    }
  }

  .secondaryCta {
    margin-left: 2rem;
  }

  .primaryCta {
    display: flex;
    align-items: center;
  }
}
