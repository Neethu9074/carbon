@import '~in-themes/active.less';

:local {
  .dialog {
    width: 66.25rem;
    &.advancedMode {
      height: 80vh;
    }
  }

  .slideInContainer {
    padding: 0 1.375rem;
  }

  .switchModeControl {
    color: @lib__colors__teal800;

    &:hover {
      cursor: pointer;
    }
  }
}
