@import '~in-themes/active.less';

:local {
  .header {
    position: relative;
  }

  .content {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: space-between;
  }

  .cardWithStrongShadow {
    box-shadow: @lib__shadows__strong;
  }

  .dragHandleIcon {
    cursor: grab;
    margin-left: 0.5rem;

    fill: @lib__colors__N300;
    &:hover {
      fill: @lib__colors__N400;
    }
  }

  .link {
    margin: 1.5rem 0 0.75rem;

    font-size: 0.875rem;
    font-weight: 600;
  }

  .linkPlaceHolder {
    height: 1rem;
  }
}
