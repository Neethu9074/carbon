/*
 * IBM Confidential
 * PID 5737-N85, 5900-AG5
 * Copyright IBM Corp. 2023
 */

:local {
  .ActionInstanceDetailsTable {
    border: 1px solid var(--ids-color-option-neutral-400);
    margin: 0px 50px 50px 50px;
    width: 90%;
    table-layout: fixed;

    tr {
      background: #fff;
    }

    tr:nth-child(even) {
      background: var(--ids-color-option-neutral-50);
    }

    th,
    td {
      color: var(--ids-color-option-neutral-900);
      font-size: 0.875rem;
      font-weight: normal;
      padding: 0.8125rem 0;
      padding-left: 1rem;
      padding-right: 1rem;
      border-top: 1px solid var(--ids-color-option-neutral-400);
      border-bottom: 1px solid var(--ids-color-option-neutral-400);
      text-align: initial;
      width: 40%;
      word-wrap: break-word;
    }

    th {
      color: var(--ids-color-option-neutral-700);
    }

    .headerRow td {
      color: var(--ids-color-option-neutral-700);
      font-size: 0.75rem;
      font-weight: normal;
    }

    td {
      padding-top: 1rem;
      padding-bottom: 1rem;
    }
  }

  .paramsTab {
    margin: 0rem 3rem 3rem 3rem;
  }

  .detailDialog {
    section {
      width: auto;
      max-width: 60vw;
      min-width: 40vw;
    }
    .detailsLink {
      display: flex;
      align-items: center;
    }

    .singleHostLimit {
      padding-left: 0px;
    }
  }

  .ActionLaneTable {
    width: auto;
    td,
    th {
      padding-top: 0px;
      padding-bottom: 0px;
      height: 2.5rem;
      a {
        svg {
          position: relative;
          top: 4px;
        }
      }
    }
  }
}
